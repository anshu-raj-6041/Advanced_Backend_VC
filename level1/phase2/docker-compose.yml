services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    environment:
      - PORT=7000
    ports:
      - 8000:7000

  frontend:
    build: ./frontend
    ports:
      - 5173:5173


  redis:
    image: redis:latest
    ports:
      - 6379:6379