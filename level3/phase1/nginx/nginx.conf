events{}


http{

    upstream backend {
        server server1:7000;
        server server2:7000;
        server server3:7000;
    }

    server{
        listen 80;
        location /{
            # return 200 "Hello from nginx Anshu";
            proxy_pass http://backend;

        }
    }
}